# 1. Block malicious scripts from running during install
ignore-scripts=true

# 2. Mimic pnpm's isolated layout (prevents "phantom" dependencies)
global-style=true

# 3. Enforce strict versioning and engine checks
engine-strict=true

# 4. Fail builds if vulnerabilities of a certain level are found
audit-level=high
