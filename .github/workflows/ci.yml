name: CI

on:
  push:
  pull_request:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Test (Node ${{ matrix.node-version }}, Pi ${{ matrix.pi-version }})
    runs-on: ubuntu-latest
    # Per-test bounds exist, but nothing the runner enforces bounds vitest worker
    # shutdown or a wedged tmux server; without this a hang burns the 360 min
    # default across both matrix legs on every push and pull_request.
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        node-version: ["24"]
        pi-version: ["0.80.6", "latest"]
    # Pi SDK 0.80.6 currently requires Node >=22.19; the package's Node 18
    # install/entrypoint contract is guarded by the package-node18 job below.
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: npm ci
      - name: Check formatting
        run: npm run format:check

      - name: Install selected Pi SDK
        env:
          PI_VERSION: ${{ matrix.pi-version }}
        run: >-
          npm install --no-save --package-lock=false
          "@earendil-works/pi-agent-core@${PI_VERSION}"
          "@earendil-works/pi-ai@${PI_VERSION}"
          "@earendil-works/pi-coding-agent@${PI_VERSION}"
          "@earendil-works/pi-tui@${PI_VERSION}"

      - name: Verify extension loads in selected Pi
        run: |
          npx --no-install pi --version
          PI_OFFLINE=1 npx --no-install pi --offline --no-session \
            -e ./src/subagent.ts --help

      - name: Install tmux
        run: |
          sudo apt-get update
          sudo apt-get install -y tmux

      # zellij is not in apt, so pull the prebuilt musl binary from the release
      # the backend is verified against. Without a real zellij on PATH the whole
      # zellij suite is mocked, which is exactly how a `dump-screen` argv that
      # zellij rejects outright shipped with CI green.
      - name: Install zellij
        env:
          ZELLIJ_VERSION: v0.44.3
        run: |
          set -euo pipefail
          curl -fsSL -o /tmp/zellij.tar.gz \
            "https://github.com/zellij-org/zellij/releases/download/${ZELLIJ_VERSION}/zellij-x86_64-unknown-linux-musl.tar.gz"
          sudo tar -xzf /tmp/zellij.tar.gz -C /usr/local/bin zellij
          sudo chmod +x /usr/local/bin/zellij
          zellij --version

      - name: Type check
        run: npm run typecheck
      - name: Run unit tests
        run: npm run test:unit
      - name: Run Pi session integration tests
        run: npm run test:pi

      - name: Run tmux integration tests
        run: npm run test:tmux
        env:
          PI_SUBAGENTURA_TMUX_SOCKET: pi-subagentura-ci-${{ github.run_id }}-${{ github.run_attempt }}
          TERM: xterm-256color

      - name: Run zellij integration tests
        run: npm run test:zellij
        env:
          TERM: xterm-256color

      # Decision (was deferred): the terminal E2E suite runs on every push, pull
      # request and the nightly cron, but only on the PINNED SDK leg. It asserts
      # on Pi's TUI paint ("Async Subagents", "Model: subagentura-e2e/mock",
      # 'Workflow "e2e-workflow" complete'), so on the floating `latest` leg any
      # cosmetic upstream change would turn master red with no local change and
      # read as a flake. Unit/integration coverage stays strict on both legs.
      - name: Run terminal visual E2E tests
        if: matrix.pi-version == '0.80.6'
        timeout-minutes: 8
        env:
          # The harness builds a replacement environment and picks its own
          # per-harness tmux socket (pid + random), so only the diagnostics
          # destination is worth setting here. Diagnostics are written on failure.
          SUBAGENTURA_E2E_DIAGNOSTICS: ${{ runner.temp }}/subagentura-e2e
        run: npm run test:tui
      - name: Upload terminal E2E diagnostics
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: terminal-e2e-${{ matrix.pi-version }}
          path: ${{ runner.temp }}/subagentura-e2e
          if-no-files-found: ignore

      - name: Format check
        run: npm run format:check

      - name: Run tests with coverage thresholds
        run: npm run coverage:check

      - name: "Verify published tarball is loadable (regression: v2.0.0 missing helpers.ts)"
        run: npx vitest run published-tarball.test.ts

      - name: Verify package can be packed
        run: npm run pack:check
  package-node18:
    name: Package install/entrypoint (Node 18)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 18

      - name: Pack package
        run: npm pack --pack-destination "$RUNNER_TEMP"

      - name: Install packed package with engine-strict
        env:
          npm_config_engine_strict: "true"
        run: |
          set -euo pipefail
          consumer="$RUNNER_TEMP/pi-subagentura-node18-consumer"
          mkdir -p "$consumer"
          printf '{"name":"packed-consumer","private":true}\n' > "$consumer/package.json"
          tgz="$(find "$RUNNER_TEMP" -maxdepth 1 -name 'pi-subagentura-*.tgz' -print -quit)"
          test -n "$tgz"
          npm install --prefix "$consumer" --omit=dev --ignore-scripts --legacy-peer-deps "$tgz"

      - name: Verify packed entrypoint contract
        working-directory: ${{ runner.temp }}/pi-subagentura-node18-consumer
        run: |
          node --input-type=module <<'NODE'
          import { access, readFile } from "node:fs/promises";

          const packageRoot = new URL("./node_modules/pi-subagentura/", import.meta.url);
          const packageJson = JSON.parse(
            await readFile(new URL("./package.json", packageRoot), "utf8"),
          );
          if (packageJson.engines?.node !== ">=18.0.0") {
            throw new Error(`unexpected package engine: ${packageJson.engines?.node}`);
          }
          if (packageJson.exports?.["."] !== "./src/subagent.ts") {
            throw new Error("package root export does not point at src/subagent.ts");
          }
          await access(new URL("./src/subagent.ts", packageRoot));
          NODE
